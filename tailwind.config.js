export default {
    // In v4 this is optional; Tailwind infers templates from Vite,
    // but it's harmless and explicit to keep:
    content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
    theme: {
        extend: {},
    },
    plugins: [],
}
